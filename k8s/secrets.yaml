# secrets.yaml
# This file is dynamically generated during CI/CD pipeline from HashiCorp Vault
# The template below shows the expected structure
# WARNING: Never commit actual secrets to version control
apiVersion: v1
kind: Secret
metadata:
  name: cs-chat-widget-secrets
  namespace: dev # Already created by Terraform, use dev or prod as needed
type: Opaque
data:
  # These values are populated from Vault during deployment (base64 encoded)
  # REDIS_PASSWORD: ""
  # PUSHER_APP_ID: ""
  # PUSHER_SECRET: ""

